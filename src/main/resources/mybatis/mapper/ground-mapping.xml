<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="companion.challeculum.domains.ground.GroundDAO">
    <delete id="deleteGround">
        delete from ground
        where id=#{groupId}
    </delete>

    <select id="showGroundDetail" parameterType="long" resultType="grounddto">
        SELECT *
        from ground
        where id = #{id}
    </select>

    <select id="getGrounds" resultType="grounddto">
        select *
        from ground
        <where>
            <if test="categoryId != null">AND category_id = #{categoryId} </if>
            <if test="level != null">AND level = #{level} </if>
        </where>
        limit #{startRow}, #{ROWS_PER_PAGE}
    </select>

    <select id="createGround" parameterType="grounddto">
        insert into ground (lecture_id, category_id, title, information, level,
                            capacity, deposit, start_at, end_at, drop_count)
        values (#{lectureId}, #{categoryId}, #{title}, #{information}, #{level},
                #{capacity}, #{deposit}, #{startAt}, #{endAt}, #{dropCount})
    </select>
</mapper>

