<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="companion.challeculum.domains.usermission.UserMissionDao">
    <!--Jonghyun-->
    <!--End of Jonghyun-->
    <!--Kiyoung-->
    <sql id="selectExtended">
        select um.id as id,
               user_id,
               mission_id,
               submit_at,
               is_accepted,
               image_url,
               ground_id,
               assignment,
               start_at,
               end_at
        from user_mission um
                 join user u on u.id = um.user_id
                 join mission m on m.id = um.mission_id
    </sql>
    <insert id="createUserMission" parameterType="UserMissionJoined">
        insert into user_mission (user_id, mission_id, image_url)
            values (#{userId},#{missionId},#{imageUrl})
    </insert>
    <insert id="insertUser"></insert>
    <select id="getUserMissionByUserIdAndMatchId" parameterType="map" resultType="UserMission">
        select *
        from user_mission
        where user_id = #{userID}
          and mission_id = #{missionID}
    </select>
    <select id="getUserMissionByUserId" resultType="Mission">
        select * from user_mission where user_id = #{userId};
    </select>


    <!--    <select id="getUserMatchByUserId" parameterType="map" resultType="UserMissionDTO">-->
    <!--        select-->
    <!--        from usser_mission um-->

    <!--    </select>-->
    <!--    <update id="updateMission" parameterType="UserMissionDTO">-->


    <!--    </update>-->
    <!--End of Kiyoung-->


    <!--Sojeong-->
    <!--End of Sojeong-->

    <!--Hwajun-->
    <!--End of Hwajun-->

    <!--HyunJoon-->
    <!--End of Hyunjoon-->
</mapper>

